# AGENTS.md

本项目是 **TG 小号矩阵管控平台**的后端（Python + Telethon + FastAPI + MySQL）。运行/技术栈/目录结构见 `README.md`。

## 关联前端

- 前端项目：**`td_topic_manager_web`**（独立仓库）。
- **前端不改**：后端接口必须严格对齐前端已有契约（路径、入参、出参），不得让前端为后端改动。
